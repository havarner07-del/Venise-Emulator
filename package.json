{
  "name": "venise",
  "version": "1.0.0",
  "description": "Venise Lua executor and emulator workbench",
  "private": true,
  "license": "MIT",
  "scripts": {
    "setup": "neu update",
    "start": "neu run",
    "build": "neu build --release && node tools/package.js",
    "server": "node server/venise-server.js",
    "test": "node server/test.js"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.2"
  }
}
